<Window x:Class="DVLD_Enterprise_System.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DVLD_Enterprise_System.UI.Views"
        mc:Ignorable="d" Title="Login" Height="450" Width="800" 
        WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" ResizeMode="NoResize" 
        AllowsTransparency="True" Background="Transparent" 
        Foreground="{StaticResource TextPrimaryBrush}">
    <Border CornerRadius="20" Background="{StaticResource SurfaceBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Image Grid.Column="0" Stretch="UniformToFill"
                   Source="/Assets/Images/login.png" HorizontalAlignment="Left" 
                   VerticalAlignment="Stretch"/>

            <Grid Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" 
                  Width="300">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0">
                    <TextBlock Text="Username" Margin="0,0,0,5"/>
                    <TextBox x:Name="UsernameInput" Height="30" Margin="0,0,0,15" 
                             VerticalContentAlignment="Center" 
                             Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="Password" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="PasswordInput" Height="30" Margin="0,0,0,25"
                                 VerticalContentAlignment="Center" 
                                 TextInput="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <Grid Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <CheckBox Grid.Column="1"/>
                    <TextBlock Text="Remember Me" Grid.Column="1" Margin="25,0,0,0"/>
                </Grid>

                <UniformGrid Columns="1" Rows="2" Grid.Row="3">
                    <Button Content="Login" Height="35" IsDefault="True" BorderThickness="0"
                            Command="{Binding LoginCommand}"/>
                    <Button Content="Cancel" Height="35" IsCancel="True" Margin="0,5,0,0" 
                            BorderThickness="0"/>
                </UniformGrid>
            </Grid>
        </Grid>
    </Border>
</Window>
